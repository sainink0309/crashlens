global:
  max_retries: 3
  max_tokens_per_request: 20000
policies:
  - id: dev-cost-guard
    match: "model =~ /gpt-4/"
    condition: "projected_cost > 0.05"
    action: downgrade
    target: gpt-3.5-turbo
alerts:
  slack: false
